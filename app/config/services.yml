parameters:
    #parameter_name: value

services:
    app.stream.sampler.service:
        class: AppBundle\Service\StreamSamplerService

    app.random.string.generator:
        class: AppBundle\Util\StringGenerator
